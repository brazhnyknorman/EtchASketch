/* Defines container*/
const container = document.querySelector(".container");
const body = document.querySelector("body");

/* Creates button element to adjust grid*/
const adjust = document.createElement("button");

/* Defines array to hold the elements*/
const divArray = [];


